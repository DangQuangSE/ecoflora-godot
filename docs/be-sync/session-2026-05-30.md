# Session Log — 2026-05-30

## 1. BE: Shop + Admin Inventory Endpoints

**Files mới:**
- `d:\WorkWithCorn\eco-backend\API\Controllers\ShopController.cs`
- `d:\WorkWithCorn\eco-backend\API\Controllers\AdminInventoryController.cs`

**Endpoints:**

| Method | Route | Role | Mục đích |
|--------|-------|------|----------|
| POST | `/api/shop/buy` | Player | Mua item, trừ currency |
| POST | `/api/admin/inventory/grant` | SuperAdmin | Tặng item vào kho, không trừ tiền |

Cả hai dùng chung `IInventoryService` (đã có sẵn). Validators `BuyItemRequestValidator` và `GrantItemRequestValidator` tự động áp dụng qua FluentValidation pipeline.

**Request body grant:**
```json
{
  "targetUserId": "uuid",
  "itemId": "uuid",           // hoặc flowerTemplateId, không dùng cả hai
  "flowerTemplateId": "uuid",
  "quantity": 5
}
```

---

## 2. Godot: Thêm 13 Variant Flowers

**Files thay đổi:**
- `autoloads/GardenManager.gd` — `_FLOWER_NAME_TO_ASSET`
- `services/ReferenceDataService.gd` — `_FLOWER_DEFAULTS`

13 hoa variant tái sử dụng asset folder của hoa gốc. Không cần thêm file PNG mới.

| BE Name | Asset Folder | Base Price | XP Tier |
|---------|-------------|------------|---------|
| golden_rose | rose | 200 | 80/220/450 |
| blue_lotus | lotus | 180 | 80/220/450 |
| rainbow_tulip | tulip | 190 | 80/220/450 |
| midnight_periwinkle | periwinkle | 170 | 80/220/450 |
| crimson_anthurium | anthurium | 210 | 80/220/450 |
| sunset_sunflower | sun_flower | 195 | 80/220/450 |
| violet_bellflower | purple_bellflower | 175 | 80/220/450 |
| moonlit_rose | rose | 250 | 120/320/600 |
| crystal_lotus | lotus | 240 | 120/320/600 |
| fire_tulip | tulip | 230 | 120/320/600 |
| silver_anthurium | anthurium | 260 | 120/320/600 |
| jade_periwinkle | periwinkle | 220 | 120/320/600 |
| star_sunflower | sun_flower | 280 | 120/320/600 |

Mỗi variant có `harvest_id` riêng (vd. `harvest_golden_rose_bloom`) để phân biệt khi thu hoạch.

---

## 3. Godot: Fix Item Icon Mapping

**File:** `autoloads/GardenManager.gd`

**Trước:** Dùng substring match trên display name (`"watering can" in raw_name`) → vỡ nếu BE đổi tên item.

**Sau:** Dùng `item["type"]` (int enum đã có sẵn trong cache):
```gdscript
const _ITEM_TYPE_ICON: Dictionary = {
    0: "res://assets/icon/watering_can.PNG",  # WATER
    1: "res://assets/icon/fertilizer.png",    # FERTILIZER
    2: "res://assets/icon/pesticide.png",     # PESTICIDE
}
```

**Cũng fix:** Xóa mapping sai `pesticide → sickle.png`. `sickle.png` là icon UI button, không phải item icon.

---

## 4. Godot: Fix InventoryManager — Mock/BE Branch

**File:** `autoloads/InventoryManager.gd`

**Bug:** `_ready()` luôn load mock inventory kể cả khi `use_mock=false`. Khi BE trả về inventory rỗng, mock data bị ghi đè → kho đồ hiển thị "Trống".

**Fix:**
```gdscript
func _ready() -> void:
    if use_mock:
        _inventory = MockInventoryService.new().get_initial_inventory()
    else:
        _inventory = UserInventory.new()  # empty, chờ fetch từ BE
        ...
```

---

## 5. Godot: Fix InventoryManager — Stale Selected Item

**File:** `autoloads/InventoryManager.gd`

**Bug:** Sau khi `_fetch_inventory()` thay `_inventory` bằng dữ liệu mới, `_selected_item` vẫn trỏ vào `InventoryItem` cũ không còn trong inventory → `consume_item` silently fail, UI slot còn hiện.

**Fix:** Clear `_selected_item` trước khi gán `_inventory = fetched`:
```gdscript
_selected_item = null
item_selected.emit(null)
_inventory = fetched
```

---

## 6. PowerShell Script: Seed Data

**File:** `d:\WorkWithCorn\eco-backend\tools\seed-data.ps1`

Script tự động:
1. Login với SuperAdmin account → lấy JWT token
2. Add 13 variant flowers vào DB (skip nếu đã tồn tại)
3. Add 14 items mới theo tier (Basic/Premium/Enchanted/Golden/Legendary × 3 loại)
4. Grant TẤT CẢ flowers + items hiện có trong DB vào inventory của target user

**Cách chạy:**
```powershell
cd d:\WorkWithCorn\eco-backend
.\tools\seed-data.ps1 -Account "admin@flowflora.dev" -Password "pw" -TargetUserId "uuid"
```

---

## 7. Data: Grant Items cho Test User

Đã grant 20 flowers + 20 items vào inventory của user:
```
a9e4c9a4-b715-47fc-88dd-10952fbfc156
```

Quantity: mỗi flower ×5, mỗi item ×10.

**Lưu ý:** Pesticide items hiện hiện `bag.png` fallback vì chưa có file `assets/icon/pesticide.png`. Cần thêm file này để hiện icon riêng.

---

## 8. Quy tắc mới (lưu vào memory)

Không dùng seeder hay mock data để test. Luôn add data thật qua API:
- Flowers → `POST /api/flowertemplates` (Admin/SuperAdmin)
- Items → `POST /api/items`
- Grant → `POST /api/admin/inventory/grant`
